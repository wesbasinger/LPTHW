# First, split this string into a list and assign it to the variable
# delimited.

quote = "I only regret that I have but one life to lose for my country"

delim = 

# Now (preferably using the .pop method), modify delim so that
# it does not have the last 5 words of the quote



# Now, join those back into a string, separated by ~'s.
# Assign it to the variable joined.

joined =
